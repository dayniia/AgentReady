# Gemini (server-only — never expose to the client)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.5-flash

# Optional: raises GitHub API rate limits when fetching public repos
GITHUB_TOKEN=

# CORS allowlist for the scan API (defaults to http://localhost:3000)
APP_ORIGIN=http://localhost:3000
