# Tiny Next.js API samples used by extractor tests. Not part of the running app.

`nextjs-mini-api/` covers App Router `route.ts` files, Pages Router `pages/api`, dynamic segments, and files that must be ignored.
