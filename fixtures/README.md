# Tiny Next.js API samples used by extractor tests. Not part of the running app.

`nextjs-mini-api/` covers App Router `route.ts` files, Pages Router `pages/api`, dynamic segments, and files that must be ignored. The running Harbor Table demo lives in the Next.js app at `/demo` and is what workspace/GitHub scans should classify — ingest skips this `fixtures/` folder.
